{% extends 'base.html.twig' %}
{% block body %}
    {% include 'include/admin_navbar.html.twig' %}
    <section class="home">
        {% include 'include/navbar_top.html.twig' %}
        <div class="container-fluid">
            <div class="card">
                <div class="card-header">
                    <div class="row">
                        <div class="col-lg-6"><h4 class="card-title" id="hidden-label-round-controls">Controladores</h4>
                        </div>
                    </div>
                </div>
{#                <div class="row">#}
{#                    <div class="col-lg-6">#}
{#                        <div class="card">#}
{#                            <div class="card-body" id="container">#}
{#                            </div>#}
{#                        </div>#}
{#                    </div>#}
{#                </div>#}

                <div class="page-content page-container" id="page-content" style="margin-top: 15px; width: auto !important;">
                    <div class="padding">
                        <div class="row container d-flex">

                            <div class="col-lg-10">
                                    <div class="card-header" style="background-color: #9212CE; width: 100% !important;">
                                        <h4 class="card-title" style="color: white">Projetos Relacionados</h4>
                                    </div>
                                    <div class="card-body">
                                            <table class="table table-hover table-striped table-dataTables">
                                                <thead>
                                                <tr>
                                                    <th>#</th>
                                                    <th>Projeto</th>
                                                    <th>Data Inicial</th>
                                                    <th>Data Final</th>
                                                    <th>Situação</th>
                                                </tr>
                                                </thead>
                                                <tbody id="tblprojetos">
                                                </tbody>
                                            </table>
                                    </div>
                            </div>
                        </div>
                    </div>
                </div>
                <div class="form-group">
                    <div class="col-lg-12 ">
                        <button class="btn btn-primary" type="button" onclick="buscarProjetos()" style="float: right !important;">Buscar Projetos</button>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <script src="{{ asset('styles/JS/dados.js') }}"></script>

    <script>
        function buscarProjetos(){

            $.ajax({
                type: 'POST',
                url: 'tratamentodados/ajaxAction',
                dataType: 'JSON',
                success: function(response) {
                    console.log(response);
                    $('.filtroTR').remove();
                    response.response.forEach((resp) => {
                        $("#tblprojetos").append
                        (`<tr class="filtroTR">
                        <td> ${resp.id} </td>
                        <td> ${resp.nome} </td>
                        <td> ${resp.data_inicial} </td>
                        <td> ${resp.data_entrega_final} </td>
                        <td> ${resp.situacao} </td>
                    </tr>`);
                    });
                }, complete: function(resp) {
                    console.log(resp);
                }
            })
        }
    </script>
{% endblock %}


