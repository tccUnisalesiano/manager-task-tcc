{% extends 'base.html.twig' %}

{% block title %}Crie sua nova Senha{% endblock %}

{% block body %}
    <h1>Crie sua nova senha</h1>

    {{ form_start(resetForm) }}
        {{ form_row(resetForm.plainPassword) }}
        <button class="btn btn-primary">Criar nova Senha</button>
    {{ form_end(resetForm) }}
{% endblock %}
